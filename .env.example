# GoatCounter count endpoint (https://www.goatcounter.com), e.g.
# https://MYCODE.goatcounter.com/count — used to enable analytics in
# production builds. Leave unset locally; set in Vercel project settings
# for the Production environment.
PUBLIC_GOATCOUNTER_URL=
